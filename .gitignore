# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
.terraform.lock.hcl

# Saved plan files embed rendered user_data (passwords) and the generated private key.
tfplan
*.tfplan

# Generated SSH keypair (private key lands here via terraform apply — never commit)
*.pem
secrets/

# OS
.DS_Store

# Local secrets (Discord bot token) — never commit
.env.local
.env
